# dependencies
node_modules/
.pnpm-store/

# env / secrets
.env
.env.local
.env.*.local

# build outputs
dist/
.next/
.next-*/
out/
build/
*.tsbuildinfo

# capacitor native projects (generated)
apps/mobile/ios/
apps/mobile/android/

# supabase
supabase/.branches/
supabase/.temp/

# misc
.DS_Store
*.log
.vscode/
.idea/
coverage/
.vercel
.claude/scheduled_tasks.lock

# /run dispatcher local dedup state (machine-local, not shared across envs)
scripts/.dispatcher-state.json

# terraform (state / tfvars は秘密情報を含むためコミットしない。
# .terraform.lock.hcl は意図的に追跡対象＝プロバイダ版を固定する)
*.tfstate
*.tfstate.*
*.tfvars
.terraform/

# playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/
