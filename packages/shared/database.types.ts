// PLACEHOLDER — フェーズ2 step2 で実DDL適用後に自動生成して上書きする：
//   supabase gen types typescript --linked > packages/shared/database.types.ts
// それまではビルドを通すための空の型定義のみ置く。
export type Database = Record<string, never>;
